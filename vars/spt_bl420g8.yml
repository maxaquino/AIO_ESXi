###
### Server Profile Template BL420c Gen8 (Bay 1)
---
server_profile_template:
  spt1:
    type: ServerProfileTemplateV2
    name: "{{ov_template}}"
    description: "{{ov_template}}"
    serverProfileDescription: "{{ov_template}}"
    serverHardwareTypeUri: "{{BL420c_Gen8_2}}"
    enclosureGroupUri: "{{EG_C7000_Test_OV}}"
    affinity: Bay
    hideUnusedFlexNics: true
    macType: Physical
    wwnType: Physical
    serialNumberType: Physical
    iscsiInitiatorNameType: AutoGenerated
    firmware:
      manageFirmware: false
      firmwareInstallType: 
      forceInstallFirmware: false
      firmwareBaselineUri: 
    connections:
    - id: 1
      name: Mgmt
      functionType: Ethernet
      networkUri: "{{matrix_40}}"
      portId: Auto
      requestedVFs: Auto
      requestedMbps: '1000'
    boot:
      manageBoot: true
      order:
      - HardDisk
    bios:
      manageBios: true
      overriddenSettings:
    localStorage:
      controllers:
      - deviceSlot: Embedded
        mode: RAID
        initialize: true
        logicalDrives:
        - name: Logical Drive 1
          raidLevel: RAID1
          bootable: true
          numPhysicalDrives: 2
          sasLogicalJBODId:
